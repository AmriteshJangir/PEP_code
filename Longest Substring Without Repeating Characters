// class Solution{
// public:
//     int lengthOfLongestSubstring(string s) {
//         vector<int> index(128, -1); // ASCII size
//         int maxLength = 0, start = -1;
//         for (int i = 0; i < s.length(); ++i) {
//             if (index[s[i]] > start) {
//                 start = index[s[i]];
//             }
//             index[s[i]] = i;
//             maxLength = max(maxLength, i - start);
//         }
//         return maxLength;
//     }
// };
